const { errorLog, infoLog } = require('../utils/console-log');

class AgentController {
  constructor() {}

  init() {
    infoLog('Build agent controller inited');
  }

  build(buildId, repoName, commitHash, buildComand) {
    infoLog('Build agent controller inited');
  }
}

module.exports = AgentController;
